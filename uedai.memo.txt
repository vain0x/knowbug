
・HSP3.x 向け拡張ランタイムの作成

動かないので、草案ということで……。

★OpenHSPのコードを流用します
1	必要なファイルをプロジェクトに追加
	・プラグインのソース
		一意なファイル名に直す( **_main などにしておくと楽 )
	・hsp3plugin.cpp および .h
	
2	拡張プラグインに使用したコードを修正
	DllMain()関数は削除する。
	EXPORTされている関数を、hsp3typeinit_** と改名し、EXPORT や WINAPI を外す。
	　また、main.h の方に extern でプロトタイプ宣言を追加。
	　extern void hsp3typeinit_**(HSP3TYPEINFO *info);
	main.h には、標準ヘッダと hsp3code.h の include だけにする。
	必要なモノは、main.cpp か cmd.h に移動。
	　hsp3plugin.h は、cppでincludeすること！！
	OpenHSP から引用した関数を削除しておくのを忘れない。
	
	hsp3sdk_init を展開する。
		ctx    = info->hspctx;
		exinfo = info->hspexinfo;
		type   = exinfo->nptype;
		val    = exinfo->npval;
	
3	OpenHSP の方のソースを、若干改変
	hsp3struct.h の #define を一部変更。
		プラグイン用の番号を定義しておく。( #define TYPE_** 18 )
		TYPE_USERDEF を 19 に。
	hsp3code.cpp で、main.h を hsp3int.h の次に include。
	code_init関数の hsp3typeinit_** 関数の羅列に肩を並べる。
	
4	少し落ち着いて、プロジェクトの設定
	・出力ファイル名の変更。
	
5	コンパイル
6	実行テスト
	動きません。。。
